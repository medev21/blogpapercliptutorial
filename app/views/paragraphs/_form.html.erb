<%= simple_form_for([@post, @post.paragraphs.build], html: { multipart: true }) do |form| %>
  <%= form.input :content, label: 'Add here' %>
  <%= form.file_field :content_image %>
  <%= form.button :submit %>
<% end %>
